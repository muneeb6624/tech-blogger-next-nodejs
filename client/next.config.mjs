/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains : ['images.pexels.com', 'c8.alamy.com', 'image.similarpng.com', 'example.com', 'localhost']
    }
};

export default nextConfig;
